version: 2
jobs:
  build:
    docker:
      - image: rust:1.29.0-stretch
    steps:
      - run:
          name: Build spdk
          command: sh ./scripts/build_spdk.sh
      - checkout
      - run:
          name: Greeting
          command: |
            rustup --version
      